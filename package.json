{
  "name": "chat-web-socket",
  "version": "1.0.0",
  "description": "1. npm init\r 2. npm install -save express\r 3. npm install -save socket.io\r 4. add \"type\": \"module\", on package.json",
  "main": "index.js",
  "type":"module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.2"
  }
}
